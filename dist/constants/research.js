// Fallback niche used for research endpoints when a user has no niche set
// (e.g. onboarding skipped). Kept broad and educational so trending results
// are still useful before the user personalizes their niche.
export const DEFAULT_NICHE = "AI and technology";
